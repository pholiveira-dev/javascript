// 
let umaString = 'O rato roeu a roupa do rei de roma ';

console.log(umaString.replace('Um', 'Outro')); // Substitui o 'Um', por 'Outro'.
console.log(umaString.length); // Vai retornar a quantidade de letras.
console.log(umaString.slice(2, 6)); // Para fatiar uma string
console.log(umaString.split(' ')); // Aqui eu pedi pra transformar em um array, separando por índice.
console.log(umaString.toUpperCase()); // Vai deixar em letra maiúscula todo o texto.
console.log(umaString.toLowerCase()); // Vai vir tudo em letra minúscula.