const produto = { nome: 'Produto', preco: 1.8 };


//Object.keys (retorna as chaves)
// Object.freeze (conjela o objeto)
// Object.assign (def, any - serve pra copiar um objeto)
// Object.getOwnPropertyDescriptor(o, 'prop')
// ... spread